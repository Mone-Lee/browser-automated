# 全局协作约定

## 注释位置

- 注释统一使用中文，避免中英文混写造成阅读和维护成本上升。
- 解释函数职责、适用边界、设计原因的注释，默认写在函数定义外面，紧贴函数上方。
- 函数内部注释只用于解释局部实现细节、特殊分支或不直观的技术处理，不重复描述整个函数“是做什么的”。
- 函数被重构、拆分或职责变化后，要同步更新函数外的注释，避免注释还在描述旧的整体逻辑。

## 函数拆分粒度

- 默认按“清晰但不过细”拆分函数，优先保证调用链容易理解，而不是机械地把每一步都抽成 helper。
- 只有在以下情况才拆分：逻辑被复用、边界更清晰、测试价值更高，或抽出后比留在原地更易读。
- 如果拆分后需要来回跳转多个很短的 helper 才能看懂主流程，通常说明拆分过细，应优先回收。
- helper 的职责应完整且稳定，避免出现“注释描述的是整体策略，但函数只剩局部碎片”的情况。

## 文件头说明

- 为新文件（除了样式文件）的顶部添加文件职能说明，概括该文件承载的职责边界与为何将这组内容放在一起。
- 文件职能说明应放在文件最顶部，作为阅读入口；当文件职责发生明显变化时，需要同步更新，避免说明落后于实现。
